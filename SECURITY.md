# Security

Report vulnerabilities privately to the repository owner. Do not include secrets, raw customer prompts, or tenant data in issues.

Security invariants include tenant-scoped data access, signed production policy bundles, frozen evaluator/safety artifacts during experiments, side-effect confirmation, secret redaction, append-only audit records, and fail-closed promotion gates.
