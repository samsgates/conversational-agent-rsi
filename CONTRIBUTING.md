# Contributing

Use conventional commits, keep interfaces provider-neutral, add tests for every policy/evaluator/runtime change, and never weaken tenant isolation or promotion gates. Run `make lint typecheck test` before opening a pull request.
